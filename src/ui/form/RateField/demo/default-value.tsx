import * as React from 'react';
import RateField from '@steroidsjs/core/ui/form/RateField';

/**
 * По-умолчанию выставленное значение
 * @order 6
 * @col 6
 */

export default () => (
    <>
        <RateField defaultValue={3} />
    </>
);
