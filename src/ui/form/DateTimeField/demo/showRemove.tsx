import * as React from 'react';
import DateTimeField from '@steroidsjs/core/ui/form/DateTimeField';

/**
 * После введения датты при наведении показывается кнопка, удалить содержание внутри инпута
 * @order 7
 * @col 6
 */

export default () => (
    <DateTimeField showRemove />
);
