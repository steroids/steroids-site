import * as React from 'react';
import TextField from '@steroidsjs/core/ui/form/TextField';

/**
 * Пример использования пропа showClose.
 * @order 1
 * @col 3
 */

export default () => (
    <>
        <TextField label='Close' layout showClose />
    </>
);
