import * as React from 'react';

import Empty from '@steroidsjs/core/ui/list/Empty';

/**
 * Basic
 * @order 1
 * @col 6
 */
export default () => (
    <>
        <Empty text={__('Ничего не найдено')} />
    </>
);
